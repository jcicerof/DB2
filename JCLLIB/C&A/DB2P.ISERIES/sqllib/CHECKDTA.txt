-- CHECK DATA TABLESPACE CX0BD.CX012T SCOPE ALL AUXERROR REPORT
--
SELECT CHAR(
       'CHECK DATA TABLESPACE '||STRIP(DBNAME)||'.'||STRIP(NAME)||
       ' SCOPE ALL AUXERROR REPORT', 80), NAME
  FROM SYSIBM.SYSTABLESPACE
 WHERE DBNAME = 'AISERIES'
;
