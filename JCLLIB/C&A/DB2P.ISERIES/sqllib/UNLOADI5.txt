-- //TESTE    EXEC PROC=UNLOADI5,TS=EQ034T,OWNER=PROD
--
SELECT CHAR('//'||CHAR(STRIP(NAME), 9)||'EXEC PROC=UNLOADI5,TS='||
       STRIP(NAME)||',OWNER=PROD', 80)
  FROM SYSIBM.SYSTABLESPACE
 WHERE DBNAME = 'AISERIES'
;
